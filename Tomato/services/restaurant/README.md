# Restaurant service

Owns restaurant business logic such as restaurant profiles, menus, availability, incoming orders, and sales.

The current API gateway exposes this role at `GET /api/restaurant/overview`. Add restaurant controllers and models here as the domain grows. Requests must use a restaurant JWT.

